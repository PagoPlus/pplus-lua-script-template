name: Luacheck

on:
  push:
    branches: main
  pull_request:
    branches: '*'

jobs:
  luacheck:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@master
      - uses: extractions/setup-just@v2
      - uses: awalsh128/cache-apt-pkgs-action@latest
        with:
          packages: lua5.3 luarocks npm
          version: 1.0
      - name: Lint
        run: just lint
      - name: Build
        run: just build